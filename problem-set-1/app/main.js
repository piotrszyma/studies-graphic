define(function (require) {
  const appConfig = require('./config');
  const domWrapper = require('./src/wrapper');
  const turtle = require('./src/turtle');
  const controller = require('./src/controller');

  console.log(domWrapper);
  console.log(turtle);
  console.log('test');
});